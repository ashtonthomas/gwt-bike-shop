package com.biker.client.common.event;

import com.google.web.bindery.event.shared.binder.GenericEvent;

public class LogoutEvent extends GenericEvent {

	public LogoutEvent(){
		// We could have paramters and stuff if we wanted
		// But that doesn't realy make sense for a logout..
	}
	
}
